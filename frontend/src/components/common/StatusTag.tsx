import { Tag } from 'antd'; export default function StatusTag({status}:{status:string}){const color=status==='on'||status==='online'||status==='handled'?'success':status==='critical'?'error':status==='warning'||status==='pending'?'warning':status==='recovered'?'cyan':'default';const text=({on:'开启',off:'关闭',online:'在线',handled:'已确认',pending:'待确认',recovered:'已恢复',warning:'预警',critical:'严重'} as Record<string,string>)[status]??status;return <Tag color={color}>{text}</Tag>}
